/**
 * Dados de estabelecimento
 */
export interface EstabelecimentoData {
  codigo: string;
  // Campos adicionais conforme necessário:
  // descricao?: string;
  // ativo?: boolean;
  // estoque?: number;
}

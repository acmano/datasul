# ============================================
# CONFIGURAÇÃO UNIFICADA - Projeto LOR0138
# ============================================
# IMPORTANTE: Use este .env.example como base
# ============================================

# ==================== SERVIDOR ====================
PORT=3000
NODE_ENV=development
API_PREFIX=/api

# ==================== BANCO DE DADOS ====================

# Tipo de conexão: 'sqlserver' ou 'odbc'
DB_CONNECTION_TYPE=sqlserver

# SQL Server - Conexão
DB_SERVER=10.105.0.4\LOREN
DB_PORT=1433
DB_USER=dcloren

# ✅ SENHA COM CARACTERES ESPECIAIS: Use aspas simples!
# Se a senha tem #, ela DEVE estar entre aspas simples
DB_PASSWORD='#dcloren#'

# ✅ DATABASE VAZIO = Usa database padrão do SQL Server user
# Deixe vazio para conectar no database padrão configurado para o usuário
# ForÃ§ar um database específico pode causar erro de permissão
DB_DATABASE_EMP=
DB_DATABASE_MULT=

# ❌ NÃO USE MAIS ESTAS VARIÁVEIS (deprecated)
# DB_NAME_EMP=emp
# DB_NAME_MULT=mult

# ✅ TIMEOUTS: Aceita 3 formatos
# - Número puro: 30000 (milissegundos)
# - Com 's': 30s (segundos, convertido para ms)
# - Com 'ms': 30000ms (milissegundos explícitos)
#
# RECOMENDADO: Use milissegundos puros para evitar confusão
DB_CONNECTION_TIMEOUT=15000  # 15 segundos para conectar
DB_REQUEST_TIMEOUT=30000     # 30 segundos para queries

# SQL Server - Segurança
DB_ENCRYPT=false
DB_TRUST_SERVER_CERTIFICATE=true

# ODBC (se DB_CONNECTION_TYPE=odbc)
ODBC_DSN_EMP=PRD_EMS2EMP
ODBC_DSN_MULT=PRD_EMS2MULT
ODBC_CONNECTION_TIMEOUT=15000

# Mock Data (desenvolvimento/testes)
USE_MOCK_DATA=false

# ==================== CORS ====================

# ✅ VARIÁVEL CORRETA (não CORS_ORIGIN)
# Múltiplas origens separadas por vírgula
CORS_ALLOWED_ORIGINS=http://lor0138.lorenzetti.ibe:3000,http://localhost:3000

# ❌ NÃO USE (deprecated)
# CORS_ORIGIN=http://localhost:3000

# ==================== TIMEOUTS HTTP ====================

# Timeout padrão de requisições HTTP
HTTP_REQUEST_TIMEOUT=30s

# Timeout para operações pesadas
HTTP_HEAVY_TIMEOUT=60s

# Timeout para health check (deve ser rápido)
HTTP_HEALTH_TIMEOUT=5s

# ==================== CACHE (Redis) ====================

# Habilitar cache
CACHE_ENABLED=true

# Estratégia de cache:
# - 'memory': Cache apenas em memória (servidor único)
# - 'redis': Cache apenas no Redis (múltiplos servidores)
# - 'layered': L1 (memória) + L2 (Redis) - RECOMENDADO
CACHE_STRATEGY=layered

# URL do Redis (se CACHE_STRATEGY=redis ou layered)
CACHE_REDIS_URL=redis://localhost:6379

# TTL padrão do cache (pode usar 's', 'ms', ou 'm' para minutos)
CACHE_DEFAULT_TTL=5m  # 5 minutos (ou '300s' ou '300000ms' ou '300000')

# ==================== LOGGING ====================

# Nível de log: error, warn, info, http, debug
LOG_LEVEL=debug

# ============================================
# OBSERVAÇÕES IMPORTANTES
# ============================================
#
# 1. SENHAS COM #:
#    ✅ DB_PASSWORD='#senha#'
#    ❌ DB_PASSWORD=#senha#  (# é comentário)
#    ❌ DB_PASSWORD="#senha#" (aspas duplas incluem nas senha)
#
# 2. DATABASE VAZIO:
#    ✅ DB_DATABASE_EMP=  (usa default do user)
#    ❌ DB_DATABASE_EMP=emp (força database, pode não ter permissão)
#
# 3. TIMEOUTS:
#    ✅ 30000 ou 30s ou 30000ms (todos funcionam)
#    ❌ Não misture formatos no mesmo projeto
#    📌 RECOMENDADO: Use apenas milissegundos (30000)
#
# 4. CACHE:
#    - 'memory': Rápido, mas não compartilha entre instâncias
#    - 'redis': Compartilhado, mas mais lento que memória
#    - 'layered': Melhor dos dois mundos (L1 + L2)
#
# 5. CORS:
#    - Use CORS_ALLOWED_ORIGINS (plural)
#    - Múltiplas origens: separadas por vírgula
#    - Não use CORS_ORIGIN (deprecated)
#
# ============================================